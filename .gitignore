# Data files
*.csv
# Parquet files are tracked (shared via git)
# Unsplit version is too large for GitHub — use the part1/part2 files instead
data/polymarket_ml_dataset_with_trends.parquet

# Notebooks
testing.ipynb
.ipynb_checkpoints/

# Model artifacts (large binaries)
models/*/artifacts/*.pkl
models/*/artifacts/*.joblib
models/*/artifacts/*.txt
models/*/artifacts/*.bin
models/*/artifacts/*.parquet

# Results JSON (keep these — small and useful to share)
# models/*/results/*.json  ← intentionally NOT ignored

# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# EDA
eda.ipynb
.DS_Store
